export async function peabindStreamFrontend({idl}) {
    /*let projectName=path.basename(output).slice(0,-4);
    let builder=createPeabindJsvalBuilder({
        idl, 
        prefix,
        projectName,
        //include: ["jsval-quickjs.h",projectName+".h"]
    });*/
}